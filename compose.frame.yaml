# compose.frame.yaml — rangka Amali 2 (Docker 4 LO2: susun sendiri).
# Nama service + indentasi SUDAH disediakan supaya tiada ralat whitespace.
# Isi setiap blok mengikut label (lihat kad blok slaid), simpan, kemudian:
#   docker compose up -d
# Hasil yang betul = compose.yaml kanonik di repo ini.
services:
  profile:
    # isi blok: image redis + volumes me:/data
  server:
    # isi blok: image ghcr.io/infratify/arena-server
  avatar:
    # isi blok: image arena-avatar + ports 8080 + COLOR/SERVER: server:3000 + depends_on profile, server
volumes:
  # isi blok: me
